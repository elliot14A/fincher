# Fincher — GSD Verify Report (Follow-up)

* **Scope**: Follow-up on `.agents/reviews/05-api-prefix-openapi/20260821T201730Z.md` — verifying Findings 1–3 are resolved
* **Generated**: `2026-08-22T00:00:00Z`
* **Skill**: `.agents/skills/gsd-verify/SKILL.md`
* **Note**: Finding C (docker-compose ClickHouse bind mount) remains intentionally deferred, per [[fincher-review-exclusions]] — build/vet/test scoped to `./internal/... ./pkg/... ./cmd/... ./openapi/...` as before.

Uncommitted, still on top of `f072629` (same changeset as the prior report, now with fixes applied on top).

---

## 1. Static Analysis & Compilation

| Check | Result |
|---|---|
| `go build ./internal/... ./pkg/... ./cmd/... ./openapi/...` | ✅ PASS |
| `go vet ./internal/... ./pkg/... ./cmd/... ./openapi/...` | ✅ PASS |
| `gofmt -l .` (excluding generated `internal/turso/ent/`) | ✅ 0 files |
| `go test -race ./internal/... ./pkg/... ./cmd/... ./openapi/...` | ✅ PASS — including new `openapi` package tests |

## 2. Finding-by-finding verification

### Finding 1 [Critical] — empty OpenAPI spec → **RESOLVED**

`openapi/generate.go` now reads:
```go
//go:generate go run github.com/swaggo/swag/cmd/swag@v1.16.4 init -g cmd/fincher/main.go -d ../ --outputTypes yaml,json -o . --parseInternal --parseDepth 5
```
This matches the fix direction from the prior report (search dir now covers the repo, `--parseInternal` set). Verified the regenerated `openapi/swagger.json` directly:
```
paths: 13
definitions: 20
```
All 13 expected route templates are present (`/titles`, `/titles/{id}`, `/masters`, `/masters/{id}`, `/vendors`, `/vendors/{id}`, `/packages`, `/packages/{id}`, `/deliveries`, `/deliveries/{id}`, `/dependencies`, `/dependencies/{id}`, `/dependencies/graph/{title_id}`).

Beyond the fix itself, a new `openapi/spec_test.go` was added that parses the embedded `SpecJSON`, asserts non-empty `paths`/`definitions`, and explicitly checks all 13 expected path keys exist — this is exactly the regression guard the prior report suggested ("Add a one-line CI/test check that fails if `openapi/swagger.json`'s `paths` is empty"), and it passes.

### Finding 2 [Low] — unused dependency surface (`ogen`, `go-faster/*`, OpenTelemetry) → **RESOLVED**

`grep -nE "ogen-go|go-faster|opentelemetry" go.mod` now returns nothing. `go.mod`'s diff against the prior state shows only genuinely-used additions (`alecthomas/kong` promoted to direct — it's the CLI-flags library actually used by `cmd/fincher`; a few indirect version bumps and new indirects like `dlclark/regexp2`, `fatih/color`, `golang.org/x/sync` that trace back to the swag toolchain, not orphaned unrelated packages). The dependency surface is now clean.

### Finding 3 [Low] — unpinned `swag@latest` → **RESOLVED**

`go:generate` now pins `swag@v1.16.4` (confirmed above) instead of `@latest`. Reproducible going forward.

---

## Summary Verdict

**PASS.** All three findings from the prior review are resolved: the OpenAPI spec is now correctly generated (13 paths, 20 definitions) and covered by a new regression test, the go.mod dependency surface is clean, and the generator version is pinned. Build/vet/gofmt/tests are all green across the same scope as before.

No new issues surfaced during this follow-up.
