# Fincher Operations Console — Frontend Environment
# In local development, leave unset to use Vite's built-in /api proxy to http://localhost:8080.
# For production or custom remote backend targets:
# VITE_API_URL=https://api.fincher.example.com/api
